mixin review(review)
  .review__header
    .review__author
      p Author
    .review__stars(title=`Rated ${review.rating} out of 5 stars`)
     review.rating
    time.review__time(datetime=review.created)= review.created
  .review__body 
    p= review.text